I have used decision tree because data is in if else like condition pattern

Requirements - Python

to run the application download the codes and open terminal and type
"pip install -requirements.txt"

then after its completion type
"python app.py"

then goto your browser and type
"localhost:5000" and hit enter

fill the values and then click on predict to get output